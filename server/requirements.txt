# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.1

# 配置管理
pydantic==2.7.4
pydantic-settings==2.3.4

# 数据库
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
pgvector==0.3.0

# Redis
redis[hiredis]==5.0.4

# HTTP 客户端（调用 LLM API）
httpx==0.27.0

# TTS
edge-tts==6.1.10
# TTS 离线降级（pyttsx3 + pydub，edge-tts 不可用时自动切换）
pyttsx3==2.99
pydub==0.25.1
# ffmpeg 需单独安装（pydub 依赖），Windows 可用 winget install ffmpeg

# ASR - 语音识别（三选一，按 ASR_BACKEND 配置选择）
# simple 后端：零配置，使用 Google Web Speech API
SpeechRecognition==3.10.1
# whisper 后端：faster-whisper，CPU 可运行 small 模型
# faster-whisper==1.0.3
# funasr 后端：阿里达摩院 FunASR，中文最优
# funasr==1.0.27

# 日志
loguru==0.7.2

# 工具
python-dotenv==1.0.1
